<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tema4acdat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.bizizaragoza.BiziActivity" />
        <activity android:name=".ui.TiempoJSONActivity" />
        <activity android:name=".ui.TiempoJSONXMLActivity" />
        <activity android:name=".ui.DivisasActivity" />
        <activity android:name=".ui.datospublicos.DatosActivity" />
        <activity android:name=".ui.datospublicos.BiblitecaActivity" />
        <activity android:name=".ui.bizizaragoza.EstacionActivity"></activity>
    </application>

</manifest>